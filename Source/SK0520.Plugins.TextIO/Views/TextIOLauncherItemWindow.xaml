<Window 
    x:Class="SK0520.Plugins.TextIO.Views.TextIOLauncherItemWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:system="clr-namespace:System;assembly=mscorlib"

    Title="TextIOLauncherItemWindow" 
    Height="450" 
    Width="800"
    WindowStartupLocation="CenterScreen"
    ResizeMode="CanResizeWithGrip"
    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
>
    <Window.Resources>
        <system:Double x:Key="GridSplitter">3</system:Double>
        <Thickness x:Key="CommonThickness">3</Thickness>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <ListBox Grid.Row="0" Margin="{StaticResource CommonThickness}" />

                <GridSplitter Grid.Row="1" Height="{StaticResource GridSplitter}" />

                <GroupBox Grid.Row="2" Margin="{StaticResource CommonThickness}" />
            </Grid>

            <GridSplitter Grid.Column="1" Width="{StaticResource GridSplitter}" />

            <Grid Grid.Column="2">

            </Grid>
        </Grid>


        <StatusBar Grid.Row="1">
            <CheckBox>topmost</CheckBox>
        </StatusBar>
        

    </Grid>
</Window>
